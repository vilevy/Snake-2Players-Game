class Fruits {
  constructor(x, y, type) {
    this.x = x;
    this.y = y;
    this.type = type;
    this.element = 'fruit';
    // this.checkElement();
  }

  // checkElement() {
  //   this.element = this.type === 'good' ? 'fruit' : 'obstacle';
  // }
}
